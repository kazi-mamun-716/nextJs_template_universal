/**
 * Breadcrumb navigation showing the current page hierarchy.
 */
export function Breadcrumb({ items }: { items: { label: string; href?: string }[] }) {
  return <nav>Breadcrumb Placeholder</nav>;
}
