import type { ReactNode } from "react";

/**
 * Shared email layout wrapper with consistent header and footer.
 * Used by all email templates for brand consistency.
 */
export function EmailWrapper({ children }: { children: ReactNode }) {
  return <div>EmailWrapper Placeholder</div>;
}
