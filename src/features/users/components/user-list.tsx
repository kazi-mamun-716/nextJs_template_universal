import type { IUser } from "@/features/users/types";

/**
 * Displays a paginated list of users.
 */
export function UserList({ users }: { users: IUser[] }) {
  return <div>UserList Placeholder</div>;
}
